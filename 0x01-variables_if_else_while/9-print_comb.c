#include <stdio.h>

/**
 * main - Prints a serie of numbers with commas
 *
 * Return: Always (Success);
 */
int main(void)
{
		int ch;

		for (ch = 48; ch <= 57; c++)
		{


			putchar{ch};
			if (ch != 57)
			{

				putchar(44);
				putchar(32);
			}
		}

			putchar(10)
}
