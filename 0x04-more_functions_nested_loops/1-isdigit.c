#include "main.h"

/**
 * _isdigit - function to check for  a digit
  * @c: an integer argument
  *
  * Return: success
  */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}
